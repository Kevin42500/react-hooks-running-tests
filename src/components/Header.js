import React from "react";
import Header from "./Header"; // Make sure to import the Header component correctly

function App() {
  return (
    <div>
      <Header text="hello from the Header!" /> {/* Provide the expected text */}
    </div>
  );
}

export default App;
